Implement the attached `label_intel/` feature into my existing DJ Organizer Toolkit.

Goal:
Add a new "Label Intelligence" feature that works without Beatport/Traxsource IDs now, but supports storing those IDs later if found.

What to do:
1. Integrate the new `label_intel/` package into the project structure cleanly.
2. Add a new CLI entry or menu action so I can run:
   - scrape label metadata from seed labels
   - export JSON, CSV, TXT, and SQLite
3. Keep the system name-first:
   - primary identity = normalized label name
   - Beatport/Traxsource IDs are optional nullable enrichment fields
4. Do not break my current pipeline.
5. Reuse existing config/output conventions already in my toolkit where sensible.
6. If my toolkit already has:
   - a main CLI
   - config loading
   - export folders
   then wire this feature into those rather than creating a separate disconnected script.
7. Add sensible defaults:
   - output folder under the toolkit data/output structure
   - cache folder under the toolkit cache structure
8. Add one example command in the README or help text for how to run it.
9. If there is already a track scanning step in my toolkit, add a follow-up hook so this label database can later be enriched from local library metadata.
10. Keep changes minimal, clean, and production-friendly.

Constraints:
- Keep Beatport and Traxsource support optional and conservative.
- Do not hard-require IDs.
- Do not redesign the whole app.
- Prefer small targeted edits over big refactors.
- Preserve Windows compatibility where the toolkit already supports it.

Expected deliverables:
- Edited project files integrated into the toolkit
- Any needed import wiring
- Any needed CLI/menu wiring
- Short summary of exactly what changed
- Example commands to use it

Suggested feature naming:
- "Label Intelligence"
- commands like:
  - `label-intel scrape`
  - or a subcommand consistent with the existing toolkit style

Files provided:
- `label_intel/__init__.py`
- `label_intel/models.py`
- `label_intel/utils.py`
- `label_intel/store.py`
- `label_intel/exporters.py`
- `label_intel/sources/base.py`
- `label_intel/sources/beatport.py`
- `label_intel/sources/traxsource.py`
- `label_intel/scraper.py`
- `label_intel/enrich_from_library.py`
- `label_intel/cli.py`
- `requirements-label-intel.txt`
- `seeds_example.txt`

Important note:
The HTML selectors for Beatport/Traxsource may need adjustment later. Keep the implementation modular so source parsers are easy to update.
